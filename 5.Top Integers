using System;
using System.Linq;

namespace _5._Top_Integers
{
    class Program
    {
        static void Main(string[] args)
        {
            var arrayInput = Console.ReadLine()
                .Split()
                .Select(int.Parse)
                .ToArray();


            for (int currentInteger = 0; currentInteger < arrayInput.Length; currentInteger++)
            {

                bool isTopInteger = true;

                for (int integerToCheck = currentInteger + 1; integerToCheck < arrayInput.Length; integerToCheck++)
                {
                    if (arrayInput[currentInteger] <= arrayInput[integerToCheck])
                    {
                        isTopInteger = false;
                        break;
                    }
                }

                if (isTopInteger)
                {
                    Console.Write(arrayInput[currentInteger] + " ");
                }
            }
        }
    }
}
